'use strict';

var Utils = require('./utils');

var d3 = require('d3-scale');

var gradient = require('./funcs/gradient');

var funcs = { gradient: gradient };

var getFunc = function getFunc(name) {
  return funcs[name];
};

var generate = function generate(d) {
  var type = d.type,
      value = d.value;

  var func = funcs[type];
  return func.generate(value);
};

module.exports = { generate: generate, funcs: funcs };